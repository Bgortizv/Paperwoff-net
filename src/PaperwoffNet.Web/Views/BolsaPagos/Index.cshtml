@model IEnumerable<PaperwoffNet.Infrastructure.BolsaPagos>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table table-striped projects dt-responsive nowrap">
    <thead>
        <tr role="row"> 
            <th>
                @Html.DisplayNameFor(model => model.TotalAbonos)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TotalDescuentos)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EstudiantesIdEstudiantesNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TotalAbonos)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TotalDescuentos)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EstudiantesIdEstudiantesNavigation.UsersIdUserNavigation.Nombre)&nbsp;
                @Html.DisplayFor(modelItem => item.EstudiantesIdEstudiantesNavigation.UsersIdUserNavigation.Apellidos)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdBolsaPagos" class="btn btn-outline-primary btn-sm">
                    <i class="fa fa-paw"></i>
                    Edit
                </a>
                <a asp-action="Details" asp-route-id="@item.IdBolsaPagos" class="btn btn-info btn-sm">
                    <i class="fa fa-circle"></i>
                    Details
                </a>
                <a asp-action="Delete" asp-route-id="@item.IdBolsaPagos" class="btn btn-danger btn-sm">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
